{
  "name": "budget",
  "version": "1.0.0",
  "description": "Cette application web permet de lister toutes ses dépenses et de faire des prévisionnels pour gérer au mieux son budget.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind:watch": "tailwindcss -i ./budget/static/budget/css/input.css -o ./budget/static/budget/css/output.css --watch",
    "tailwind:build": "tailwindcss -i ./budget/static/budget/css/input.css -o ./budget/static/budget/css/output.css --minify",
    "build": "npm run tailwind:build && python manage.py collectstatic --noinput"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maximerose/budget.git"
  },
  "keywords": [],
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/maximerose/budget/issues"
  },
  "homepage": "https://github.com/maximerose/budget#readme",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
